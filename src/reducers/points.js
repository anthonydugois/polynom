import * from "../constants/ActionTypes"

function M(state = {
  code: "M",
  x: 0,
  y: 0,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function L(state = {
  code: "L",
  x: 0,
  y: 0,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function Q(state = {
  code: "Q",
  x: 0,
  y: 0,
  x1: 0,
  y1: 0,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function T(state = {
  code: "T",
  x: 0,
  y: 0,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function C(state = {
  code: "C",
  x: 0,
  y: 0,
  x1: 0,
  y1: 0,
  x2: 0,
  y2: 0,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function S(state = {
  code: "S",
  x: 0,
  y: 0,
  x2: 0,
  y2: 0,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function A(state = {
  code: "A",
  x: 0,
  y: 0,
  rx: 0,
  ry: 0,
  xAxisRotation: 0,
  largeArc: false,
  sweep: false,
}, action) {
  switch (action.type) {
  default:
    return state
  }
}

function point(state = {
  id: 0,
  isActive: false,
  isRelative: false,
  parameters: {},
}, action) {
  switch (action.type) {
  default:
    return state
  }
}
